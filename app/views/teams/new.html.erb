<div class="container mx-auto p-4">
  <h1 class="text-2xl font-bold mb-4">Novo Time</h1>
  <%= form_with model: @team, class: "space-y-4" do |form| %>
    <div>
      <%= form.label :name, "Nome", class: "block text-sm font-medium" %>
      <%= form.text_field :name, class: "mt-1 block w-full p-2 border rounded-md" %>
    </div>
    <div>
      <%= form.label :division, "Divisão", class: "block text-sm font-medium" %>
      <%= form.text_field :division, class: "mt-1 block w-full p-2 border rounded-md" %>
    </div>
    <div>
      <%= form.submit "Criar Time", class: "bg-green-600 text-white p-2 rounded-md hover:bg-green-700" %>
    </div>
  <% end %>
</div>
